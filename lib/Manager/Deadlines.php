<?php
class Manager_Deadlines extends View {
    function init(){
        parent::init();

/*
        $this->add('H4')->set('5. Project Deadlines');
        $grid=$this->add('Grid');
        $model=$grid->setModel('Budget',array('name','priority','state','deadline'));
        $model->addCondition('accepted',true);
        $model->addCondition('closed',false);
        */
    }
}
